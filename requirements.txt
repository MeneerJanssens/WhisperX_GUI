--extra-index-url https://download.pytorch.org/whl/cu129

customtkinter==5.2.2
whisperx==3.7.2
torch==2.8.0+cu129
torchaudio==2.8.0+cu129
torchvision==0.23.0+cu129
numpy==2.2.5
soundfile==0.13.1
hf-xet==1.2.0
pyannote.audio>=3.0.0

# Notes:
# - `ffmpeg` (the CLI) is required by the audio loader used by whisper; install it separately for your OS.
# - The torch versions above include CUDA 12.9 support (+cu129).